# Grid Reader — Production dependencies (Vercel)
# PDF processing libraries excluded to stay under 250MB serverless limit.
# PDF import runs locally only. See requirements-local.txt for full list.
flask>=2.0.0
pyyaml>=6.0
requests>=2.25.0
supabase>=2.0.0
python-dotenv>=1.0.0
