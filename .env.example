# Grid Reader Environment Variables
# Copy this file to .env and fill in your values

# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Service role key for admin operations
# SUPABASE_SERVICE_KEY=your-service-key-here

# Optional: Secret key for signing trainer sessions (HMAC)
# Auto-generated if not set (sessions won't survive server restarts)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# SESSION_SECRET=your-random-secret-here

# Authentication (Supabase Auth)
# Get JWT Secret from: Supabase Dashboard > Settings > API > JWT Secret
# Required for admin routes (upload, delete). Without it, admin routes return 401.
# SUPABASE_JWT_SECRET=your-jwt-secret-here
